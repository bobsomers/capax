#ifndef BUILDCTRL_H
#define BUILDCTRL_H

// DEVMODE is defined if we are using the graphical test harness to
// emulate hardware, otherwise we're building in production mode and
// using the hardware interface service to get real data from the
// hardware

//#define DEVMODE

#endif // BUILDCTRL_H
